#include<bits/stdc++.h>
using namespace std;
int count_on_bit(int x){
    int ans=0;
for(int k=0; k<32; k++){
    if(check_kth_bit(x,k)){
       ans++;
    }
}
return ans;
}
int main(){
//cout<<check_kth_bit(11,3)<<endl;
 //print_on_bit(11); //binary 1011 -> 0 1 3
 //print_off_bit(11);

 count_on_bit(11);
}
