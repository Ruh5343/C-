#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Enter a number: ";
    cin >> n;

    int absoluteValue;

    if (num < 0) {
        absoluteValue = -n;  // make it positive
    } else {
        absoluteValue = n;   // already positive
    }

    cout << "Absolute value = " << absoluteValue << endl;

    return 0;
}
